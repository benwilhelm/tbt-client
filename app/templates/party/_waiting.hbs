  <div class="party">
    <div class="col-left">
      <div class="party-info">
  	    <div class="party-size">{{size}}</div>
  	    <div class="name">{{name}}</div>
  	    <div class="time-taken"><span>Time Taken:</span> {{hourMinute time_taken}}</div>
  	    <div class="time-promised"><span>Time Promised:</span> {{hourMinute time_promised}}</div>
  	  </div><!-- .party-info -->
    </div><!-- .half.left -->
    <div class="buttons col-right">
      {{#if notified}}
    	  <div {{action 'recall' on='click'}} class="button notify notified"><span class="label">Notified</span> <span class="countdown">4:32</span></div>
      {{else}}
    	  <div {{action 'notify' on='click'}} class="button notify">Notify <span class="phone">(847) 644-9168</span></div>
      {{/if}}
  	  <div class="button seat">Seat</div>
  	  <div class="button cancel">Cancel</div>
    </div><!-- .buttons -->
    <div class="clear"></div>
  </div><!-- .party -->

